### 🔹 Уровень 1: Структуры и методы

1. **Создание структуры**  
   Создайте структуру `Person` с полями `Name` и `Age`. Напишите метод `Greet()`, который выводит `"Hello, I'm <Name>"`.
2. **Указатели vs значения**  
   Добавьте метод `Birthday()` к `Person`, увеличивающий возраст на 1. Реализуйте его дважды: один раз с приёмником-значением, другой — с приёмником-указателем. Объясните разницу в поведении.
3. **Приватность полей**  
   Создайте структуру `bankAccount` с приватным полем `balance`. Напишите публичные методы `Deposit(amount float64)` и `GetBalance() float64`. Почему нельзя напрямую изменить `balance` извне пакета?

---

### 🔹 Уровень 2: Композиция и эмбеддинг

4. **Эмбеддинг как замена наследованию**  
   Создайте структуру `Animal` с методом `Speak()`. Создайте структуру `Dog`, эмбеддящую `Animal`, и переопределите `Speak()` так, чтобы он выводил `"Woof!"`.
5. **Композиция вместо наследования**  
   Создайте структуру `Engine` с методом `Start()`. Создайте структуру `Car`, содержащую `Engine` как поле. Реализуйте метод `Drive()`, использующий `engine.Start()`.
6. **Конфликт имён при эмбеддинге**  
   Эмбеддьте две структуры с методами одинакового имени в третью. Что произойдёт? Как вызвать нужный метод явно?

---

### 🔹 Уровень 3: Интерфейсы

7. **Базовое использование интерфейса**  
   Определите интерфейс `Shape` с методом `Area() float64`. Реализуйте его для структур `Rectangle` и `Circle`.
8. **Интерфейс как параметр функции**  
   Напишите функцию `PrintArea(s Shape)`, которая выводит площадь любой фигуры.
9. **Пустой интерфейс и type assertion**  
   Напишите функцию `Describe(v interface{})`, которая определяет тип переданного значения (`int`, `string`, `Person`) и выводит соответствующее сообщение с использованием type switch.
10. **Интерфейсная совместимость**  
    Создайте структуру `Square`, реализующую `Shape`. Передайте её в функцию, ожидающую `interface{}`. Проверьте, можно ли затем преобразовать её обратно к `Square`.

---

### 🔹 Уровень 4: Продвинутые паттерны

11. **Фабрика объектов**  
    Напишите функцию `NewPerson(Name string, age int) *Person`, инициализирующую и возвращающую указатель на `Person`.
12. **Конструктор с опциями (Functional Options)**  
    Реализуйте гибкий конструктор для структуры `Server` с полями `Port`, `Host`, `TLS`. Используйте паттерн функциональных опций.
13. **Интерфейс с контекстом**  
    Создайте интерфейс `Worker` с методом `Do(ctx context.Context) error`. Реализуйте его для `FileProcessor` и `NetworkFetcher`.
14. **Мокирование через интерфейсы**  
    Напишите интерфейс `Logger` с методом `Log(msg string)`. Создайте реализацию `StdoutLogger` и мок `MockLogger` для тестов. Напишите простой юнит-тест.

---

### 🔹 Уровень 5: Исключения и тонкости

15. **Методы на nil-приёмниках**  
    Создайте структуру `List` с методом `Len() int`, возвращающим длину. Реализуйте его так, чтобы он корректно работал даже если приёмник — `nil`. Продемонстрируйте это.
16. **Изменение полей через интерфейс**  
    Можно ли изменить внутреннее состояние объекта, если у вас только интерфейсный тип? Продемонстрируйте на примере.
17. **Интерфейс ≠ конкретный тип**  
    Объясните, почему `[]T` не реализует `[]interface{}`. Напишите функцию, преобразующую `[]int` в `[]interface{}`.
18. **"Наследование" через интерфейсы**  
    Определите интерфейс `Reader` и `Closer`. Создайте интерфейс `ReadCloser`, включающий оба. Реализуйте его для структуры `File`.